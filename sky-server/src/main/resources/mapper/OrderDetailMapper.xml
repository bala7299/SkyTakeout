<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.sky.mapper.OrderDetailMapper">

    <insert id="insertBatch">
        insert into order_detail(name, image, order_id, dish_id, setmeal_id, dish_flavor, amount)
        values
        <foreach collection="list" item="od" separator=",">
            (#{od.name}, #{od.image}, #{od.orderId}, #{od.dishId}, #{od.setmealId}, #{od.dishFlavor}, #{od.amount})
        </foreach>
    </insert>

    <select id="getTopSales" resultType="com.sky.dto.TopSalesItemDTO">
        SELECT
            MAX(dish_id) AS dish_id,
            MAX(setmeal_id) AS setmeal_id,
            name,
            MAX(image) AS image,
            MAX(amount) AS price,
            SUM(number) AS total_sales
        FROM order_detail
        GROUP BY name
        ORDER BY total_sales DESC
        LIMIT #{limit}
    </select>
</mapper>